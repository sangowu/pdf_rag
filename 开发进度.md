# RAG 项目开发进度

## 已完成

- **第一阶段：环境准备 & 数据探索**
  - [x] 环境配置
  - [x] 数据探索脚本

- **第二阶段：OCR 处理器**
  - [x] PaddleOCR-VL 1.5 集成 (`src/ocr_processor.py`)
  - [x] 批量处理多个 PDF (`scripts/run_ocr_batch.py`)
  - [x] OCR 质量评估 (`src/ocr_evaluator/`)

- **第三阶段：文本切片与 Chunk 管理**
  - [x] fixed / recursive / parent-child 三种切片策略 (`src/chunk_manager.py`)
  - [x] chunk 分布可视化 (`scripts/validate_chunk_distribution.py`)

- **第四阶段：向量存储**
  - [x] BGE-M3 本地 embedding (`src/vector_store.py`)
  - [x] ChromaDB 存储（cosine 相似度）
  - [x] BM25 稀疏检索 (`src/bm25_store.py`)
  - [x] Hybrid 检索（RRF 融合）
  - [x] bge-reranker-v2-m3 重排序 (`src/reranker.py`)

- **第五阶段：QA 数据集构建**
  - [x] LLM 模式：从 chunk 自动生成 QA (`scripts/generate_qa_from_chunks.py`)
  - [x] 双语 prompt（中文/英文自动检测）
  - [x] 低质量 chunk 过滤 + post-validation
  - [x] Qwen3-32B BNB 4-bit 量化加速

- **第六阶段：评估框架**
  - [x] Hit@K、MRR 检索评估 (`src/evaluator.py`)
  - [x] 延迟统计（embed/chroma/rerank/total）
  - [x] 自动实验对比报告 (`scripts/generate_report.py`)
  - [x] 指标可视化 (`scripts/plot_metrics.py`)

- **第七阶段：完整 Pipeline**
  - [x] 一键运行脚本 (`scripts/run_full_pipeline.py`)
  - [x] 各阶段 skip 参数支持

## 当前基线指标

| 指标 | 数值 |
|------|------|
| Hit@1 | 63.5% |
| Hit@3 | 83.0% |
| Hit@5 | 87.0% |
| MRR | 0.73 |

## 待实现

- [ ] RAG 端到端答案生成 + EM/ROUGE-L 评测 (`scripts/run_answer_eval.py`)
- [ ] Gradio 交互 Demo
