# Serving layer dependencies only (FastAPI + Gradio + Retrieval)
# Does not include OCR / chunking / evaluation dependencies

# API serving
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Gradio demo
gradio>=4.0.0

# Vector store + retrieval
chromadb>=0.4.0
rank_bm25>=0.2.0
jieba>=0.42.0

# Embeddings & reranker (BGE-M3, bge-reranker-v2-m3)
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.25.0
FlagEmbedding>=1.2.0

# LLM API (ModelScope / OpenAI-compatible)
openai>=1.0.0

# Utilities
numpy>=1.21.0
pyyaml>=6.0
tqdm>=4.65.0
requests>=2.28.0
python-dotenv>=1.0.0
